# URL of the separately deployed RAG Agent. Do not put secrets in this file.
RAG_AGENT_URL=http://localhost:8001
# Confirm this path with the RAG Agent owner before deployment.
RAG_AGENT_CHAT_PATH=/api/chat
RAG_AGENT_TIMEOUT_SECONDS=40
APP_ENVIRONMENT=development
